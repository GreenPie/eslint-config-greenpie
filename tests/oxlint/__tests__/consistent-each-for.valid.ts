import { describe, it, suite, test } from 'vitest';

describe.each([])('describe %s', () => {});
test.each([])('test %s', () => {});
it.each([])('it %s', () => {});
suite.each([])('suite %s', () => {});
